import { LibraryBase } from "../lib/libClass";

/** A product widget: the only class the product should document. */
export class ProductWidget extends LibraryBase {
  /** A product method. */
  productMethod(): void {}
}
