# Azure Cognitive Services Translator subscription key.
# Get it from the Azure portal: Translator resource -> Keys and Endpoint.
# `survey-utils translate <product> --key <key>` passes the key instead, and wins over this.
TRANSLATION_API_KEY=
